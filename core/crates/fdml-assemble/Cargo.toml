[package]
name = "fdml-assemble"
version = "0.1.0"
edition = "2021"
description = "FDML core rework — pass 6: deterministic FDML spec assembly (LinkReport + ScanResult → FDML YAML)"

[dependencies]
fdml-types = { workspace = true }
fdml-util = { workspace = true }

[dev-dependencies]
fdml-scan = { workspace = true }
fdml-graph = { workspace = true }
serde_json = { workspace = true }
tempfile = "3"
